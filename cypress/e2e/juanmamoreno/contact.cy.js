/// <reference types="cypress" />

describe('artwork page', () => {
    beforeEach(() => {
        cy.visit('https://juanmamoreno.com/contact')
    })
  
    it('should display the contact form', () => {
    })

    it('should submit the form correctly', () => {
    })
});